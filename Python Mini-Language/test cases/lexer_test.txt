// this is a comment
program lexer_test:
    int a;
    int b;
    int c;
    a := 4;
    b := 2;
    if (a > 2) then
        c := a + b;      // This is another comment
    else
        c := (a - b) * b;
        a := c / b;
        c := a * b;
    fi;
    print c     // This comment will be ignored
end


